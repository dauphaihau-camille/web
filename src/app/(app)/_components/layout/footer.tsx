export function Footer() {
  return (
    <footer className="border-t bg-background">
      <div className="mx-auto max-w-7xl px-4 py-4 text-sm text-muted-foreground">
        Shared app chrome for the `(app)` route group.
      </div>
    </footer>
  );
}
