# Web Agent Notes

Follow the focused rules in [`agents/`](./agents/).

Current rules:
- [`agents/co-located-files.md`](./agents/co-located-files.md): keep page-specific files under `app/` and reserve [`src/components/`](./src/components/) for components shared across pages.
